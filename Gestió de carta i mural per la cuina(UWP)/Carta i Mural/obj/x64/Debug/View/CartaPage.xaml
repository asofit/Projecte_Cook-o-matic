<Page
    x:Class="Carta_i_Mural.View.CartaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Carta_i_Mural.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="33*"></ColumnDefinition>
            <ColumnDefinition Width="66*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"
            BorderBrush="Black" BorderThickness="0,0,1,0" ></Border>
        <!--Títols-->
        <Border Grid.Column="0" Grid.Row="0" 
                Background="IndianRed">
            <TextBlock Text="Categories"   FontWeight="Bold" FontSize="24" TextAlignment="Center" Foreground="White"></TextBlock>
        </Border>
        <Border Grid.Column="1" Grid.Row="0"
                Background="LimeGreen">
            <TextBlock Text="Plats" FontWeight="Bold" FontSize="24" TextAlignment="Center" Foreground="White"></TextBlock>
        </Border>
        
        <!--Categories-->
        <ListView x:ConnectionId='2' x:Name="lsvCategories" Grid.Column="0" Grid.Row="1"                                                  >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:UICategoria CurrentCategoria="{Binding}"></local:UICategoria>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="0" Grid.Row="2" Background="LightGray">
            <Border BorderBrush="Black" BorderThickness="0,0,1,0" ></Border>
            <Button Content="❌Elimina la selecció" Margin="5"
                HorizontalAlignment="Center"></Button>
        </Grid>
        
        <!--Plats-->
        <ScrollViewer Grid.Column="1" Grid.Row="1">
            <GridView x:ConnectionId='4' x:Name="grvPlats">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"
                                       MaximumRowsOrColumns="3"></ItemsWrapGrid>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <local:UIPlat CurrentPlat="{Binding}"></local:UIPlat>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
        <Grid Grid.Column="1" Grid.Row="2" Background="LightGray" Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="66*"></ColumnDefinition>
                <ColumnDefinition Width="33*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox x:ConnectionId='3' Grid.Column="0" PlaceholderText="Filtra pel nom del plat"                                    ></TextBox>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="➕" Margin="5,0,5,0"></Button>
                <Button Content="➖" Margin="5,0,5,0"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

